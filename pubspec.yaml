name: flutter_ddd
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"
dependencies:
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1 # dart의 함수형 프로그래밍을 위한 유틸리티 라이브러리
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1

dev_dependencies:
  build_runner: ^2.4.6
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed:

flutter:
 
  uses-material-design: true
